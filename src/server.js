'use strict'

const Setup = require('./setup')

module.exports = Setup

// const postBulkFunc = (request) => {
//     console.log('post bulk got called');
//         return 'post bulk got called'
//     };
// const customConfig = {
//     port: 3070
//   }

//     let OracleServer = new Setup.OracleServer(null, null, null, null,postBulkFunc, customConfig)

//     let init = OracleServer.initialize()
